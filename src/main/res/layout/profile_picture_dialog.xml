<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.appcompat.widget.Toolbar
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="@color/colorPrimary"
        android:id="@+id/daily_task_dialog_toolbar">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/profile_picture_dialog_close"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="end"
            android:scaleX="0.7"
            android:scaleY="0.7"
            android:src="@drawable/ic_close_white"
            app:civ_border_color="@android:color/darker_gray"
            app:civ_border_width="4dp" />

    </androidx.appcompat.widget.Toolbar>

    <TextView
        android:id="@+id/profile_picture_dialog_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:layout_margin="16dp"
        style="@style/TextAppearance.AppCompat.Title"
        android:layout_marginTop="16dp"
        android:text="@string/choose_a_profile_picture"/>

    <androidx.recyclerview.widget.RecyclerView
        android:layout_marginTop="16dp"
        android:id="@+id/profile_picture_recycler_view"
        android:overScrollMode="never"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <Space
        android:layout_width="match_parent"
        android:layout_height="48dp"/>


</LinearLayout>